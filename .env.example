# Storage Configuration
# Options: 'aws' for Amazon S3, 'local' for local S3-compatible storage, 'sftp' for SFTP remote storage
STORAGE_TYPE=aws

# AWS S3 Configuration (when STORAGE_TYPE=aws)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=your-bucket-name
AWS_ENDPOINT=

# Local S3-Compatible Storage Configuration (when STORAGE_TYPE=local)
LOCAL_STORAGE_PATH=./storage
LOCAL_BASE_URL=http://localhost:8000

# SFTP Remote Storage Configuration (when STORAGE_TYPE=sftp)
SFTP_HOST=your-sftp-server.com
SFTP_PORT=22
SFTP_USERNAME=your-username
SFTP_PASSWORD=your-password
# OR use SSH key authentication (recommended)
# SFTP_PRIVATE_KEY=/path/to/private/key
# SFTP_PRIVATE_KEY_PASSWORD=key-passphrase-if-any
SFTP_PATH=/remote/storage/path
SFTP_BASE_URL=http://localhost:8000# AP
I Configuration (optional)
# Uncomment and set a secret key to enable API key authentication for curl uploads
# API_KEY=your-secret-api-key-for-curl-access